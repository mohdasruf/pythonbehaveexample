version: 0.2

phases:
  pre_build:
    commands:
      - echo in prebuild

  build:
    commands:
      - echo Entered the post_build phase...
      - pip install --upgrade pip
      - pip install -r requirements.txt
      - behave --tags=ENV1 -f allure_behave.formatter:AllureFormatter -o %allure_result_folder% ./features
      - scoop install allure
      - allure serve %allure_result_folder%

